<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.aigateway.mapper.IdempotencyRecordMapper">

    <sql id="BaseColumns">
        id, scope, api_key_id, idempotency_key_hash, request_fingerprint, request_id, status,
        response_json, error_code, expires_at, created_at, updated_at
    </sql>

    <insert id="insertIdempotencyRecord" parameterType="com.example.aigateway.entity.IdempotencyRecord"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO idempotency_record (
            scope, api_key_id, idempotency_key_hash, request_fingerprint, request_id, status, expires_at
        )
        VALUES (
            #{scope}, #{apiKeyId}, #{idempotencyKeyHash}, #{requestFingerprint}, #{requestId}, #{status}, #{expiresAt}
        )
    </insert>

    <insert id="insertIdempotencyRecordIgnore" parameterType="com.example.aigateway.entity.IdempotencyRecord"
            useGeneratedKeys="true" keyProperty="id">
        INSERT IGNORE INTO idempotency_record (
            scope, api_key_id, idempotency_key_hash, request_fingerprint, request_id, status, expires_at
        )
        VALUES (
            #{scope}, #{apiKeyId}, #{idempotencyKeyHash}, #{requestFingerprint}, #{requestId}, #{status}, #{expiresAt}
        )
    </insert>

    <select id="getByScopeAndIdempotencyKeyHash" resultMap="IdempotencyRecordResultMap">
        SELECT
        <include refid="BaseColumns"/>
        FROM idempotency_record
        WHERE scope = #{scope}
          AND idempotency_key_hash = #{idempotencyKeyHash}
    </select>

    <select id="getByScopeAndIdempotencyKeyHashForUpdate" resultMap="IdempotencyRecordResultMap">
        SELECT
        <include refid="BaseColumns"/>
        FROM idempotency_record
        WHERE scope = #{scope}
          AND idempotency_key_hash = #{idempotencyKeyHash}
        FOR UPDATE
    </select>

    <select id="getByRequestId" resultMap="IdempotencyRecordResultMap">
        SELECT
        <include refid="BaseColumns"/>
        FROM idempotency_record
        WHERE request_id = #{requestId}
    </select>

    <update id="updateIdempotencyRecordResult">
        UPDATE idempotency_record
        SET status = #{status},
            response_json = #{responseJson},
            error_code = #{errorCode}
        WHERE id = #{id}
    </update>

    <resultMap id="IdempotencyRecordResultMap" type="com.example.aigateway.entity.IdempotencyRecord">
        <id property="id" column="id"/>
        <result property="scope" column="scope"/>
        <result property="apiKeyId" column="api_key_id"/>
        <result property="idempotencyKeyHash" column="idempotency_key_hash"/>
        <result property="requestFingerprint" column="request_fingerprint"/>
        <result property="requestId" column="request_id"/>
        <result property="status" column="status"/>
        <result property="responseJson" column="response_json"/>
        <result property="errorCode" column="error_code"/>
        <result property="expiresAt" column="expires_at"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>
</mapper>
